name = "backend-convention-checker"
description = "Audit changed Spring Boot backend files against project conventions."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
model_instructions_file = "backend-convention-checker.md"
developer_instructions = "Read `.codex/agents/backend-convention-checker.md` from the current Git worktree before doing role work."
