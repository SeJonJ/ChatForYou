name = "backend-test-convention-checker"
description = "Audit changed backend test files against project test conventions."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
model_instructions_file = "backend-test-convention-checker.md"
developer_instructions = "Read `.codex/agents/backend-test-convention-checker.md` from the current Git worktree before doing role work."
